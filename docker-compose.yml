version: '3.8'
services:
  cibil-error-monitor:
    build: .
    image: cibil-error-monitor:latest
    container_name: cibil-error-monitor
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    networks:
      - kafka-net
    restart: unless-stopped

networks:
  kafka-net:
    external: true
